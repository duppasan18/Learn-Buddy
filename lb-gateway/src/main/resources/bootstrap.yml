spring:
  application:
    name: lb-gateway
  cloud:
    nacos:
      server-addr: 192.168.100.128:8848 # nacos注册中心
  config:
    import:
      - optional:nacos:shared-gateway-config.yaml
server:
  port: 8081