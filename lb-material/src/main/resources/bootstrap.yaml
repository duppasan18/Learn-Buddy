spring:
  application:
    name: lb-material
  profiles:
    active: dev
  config:
    import:
      - optional:nacos:shared-jdbc.yaml
      - optional:nacos:shared-jwt-properties.yaml
      - optional:nacos:shared-security.yaml
      - optional:nacos:shared-redis.yaml
      - optional:nacos:shared-oss-properties.yaml
  cloud:
    nacos:
      server-addr: 192.168.100.128:8848
server:
  port: 8086
datasource:
  name: lb_material
