spring:  
  datasource:  
    url: jdbc:mysql://${mysql.MYSQL_HOST:192.168.100.128}:${mysql.port:3306}/${datasource.name}?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true  
    driver-class-name: com.mysql.cj.jdbc.Driver  
    username: root  
    password: ${mysql.MYSQL_PASSWORD}